# Onyx Delta FFAEU2 Migration

تم تنظيف إعدادات السيرفر داخل المشروع والإبقاء على نقطة اتصال واحدة فقط:

- الاسم: Delta FFAEU2 — EU
- العنوان: `wss://eu.mi.com:2001`
- المعرّف: `delta-ffaeu2`

الملفات التي تم تحديثها تشمل قائمة السيرفر في `index.html` و`ffa/client.html`، إعدادات `onyx-unified.js`، طبقة `onyx-ffa.js` و`onyx-ffa-adapter.js`، حارسي `ffa/onyx-ffa-codec.js` و`ffa/onyx-ffa-codec-worker.js`، وقواعد العنوان المخفية في `ffa/bundle.js` وملفات التشخيص.

تم الحفاظ على واجهة Onyx، المصادقة، التابات، الإضافات، ومنطق البوت. لم يتم حذف حسابات المستخدمين أو مفاتيح المصادقة، ولم يتم حذف أي سيرفر خارجي فعلي؛ التنظيف يخص قوائم وإعدادات السيرفر داخل المشروع فقط.

> تنبيه: عنوان Delta قد يستخدم بروتوكولًا مختلفًا عن عميل Onyx/Senpa القديم. هذه النسخة تجعل Delta هو الخيار الوحيد وتمنع حراس Senpa القديمة من رفض العنوان، لكن نجاح اللعب الفعلي يعتمد على توافق الـ WASM والبروتوكول مع Delta.
